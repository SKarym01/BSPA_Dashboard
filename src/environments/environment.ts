// Default environment. `ng serve` swaps this with environment.development.ts via angular.json.
export const environment = {
  production: false,
  envName: 'default',
  apiBaseUrl: 'http://localhost:8000/api/v1',
};
